var ServerModel = Backbone.Model.extend({
  /*Member*/
  //channelList
});

var ServerCollection = Backbone.Collection.extend({
  model: ServerModel
});

var ChannelModel = Backbone.Model.extend({

});

var ChannelCollection = Backbone.Collection.extend({
  model: ChannelModel
});


//side server list view
  //foreach { channel list view }